jdk:
  - openjdk11
before_install:
  - yes | $ANDROID_HOME/cmdline-tools/bin/sdkmanager "build-tools;34.0.0"
  - yes | $ANDROID_HOME/cmdline-tools/bin/sdkmanager --uninstall "ndk-bundle" | $ANDROID_HOME/tools/bin/sdkmanager --install "ndk;25.2.9519653"
